import { SupporterWithUser } from './supporter-with-user';

describe('SupporterWithUser', () => {
  it('should create an instance', () => {
    expect(new SupporterWithUser()).toBeTruthy();
  });
});
